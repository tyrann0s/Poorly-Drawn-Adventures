using Mobs;
using UnityEngine;

namespace Items.Scrolls
{
    public class SupportScroll : ItemScroll
    {
        public override void Initialize(bool isAOE, Mob target)
        {
            base.Initialize(isAOE, target);
        }
        
        public override void Use()
        {
            base.Use();
        }
    }
}
